installed.packages()
names(installed.packages())
row.names(installed.packages())
("jsonlite" %in% row.names(installed.packages()))
!("jsonlite" %in% row.names(installed.packages()))
if (!("jsonlite" %in% row.names(installed.packages()))){
install.packages("jsonlite")
}
pkg_chk_install <- function(package_name){
if (!(package_name %in% row.names(installed.packages()))){
install.packages(package_name)
}
}
pkg_chk_install("jsonlite")
library(jsonlite)
?fromJSON
pkg_chk_install("rjson")
library(rjson)
fromJSON("pakistan_districts.topojson")
fromJSON(file = "pakistan_districts.topojson")
district <- fromJSON(file = "pakistan_districts.topojson")
district_level <- fromJSON(file = "pakistan_districts.topojson")
rm(district)
prov_level <- fromJSON(file = "pakistan_prov.topojson")
district_level$objects$pakistan_districts
district_level[['objects']]
district_level[['objects']][['pakistan_districts']]
district_level[['objects']][['pakistan_district']]
district_level[['objects']][['pakistan_districts']]
layered <- district_level
layered <- district_level
layered[['objects']][['pakistan_prov']] <- prov_level$objects$pakistan_prov
?toJSON
toJSON(layered)
write_json(toJSON(layered), path = 'pak_dist_prov.topojson')
write(toJSON(layered), path = 'pak_dist_prov.topojson')
write(toJSON(layered), file = 'pak_dist_prov.topojson')
layered$objects$pakistan_prov
pkg_chk_install <- function(package_name){
if (!(package_name %in% row.names(installed.packages()))){
install.packages(package_name)
}
}
pkg_chk_install("rjson")
library(rjson)
district_level <- fromJSON(file = "pakistan_districts.topojson")
prov_level <- fromJSON(file = "pakistan_prov.topojson")
layered <- district_level
layered[['objects']][['pakistan_prov']] <- prov_level$objects$pakistan_prov
layered$objects$pakistan_prov
write(toJSON(layered), file = 'pak_dist_prov.topojson')
district_level <- fromJSON(file = "pakistan_districts.topojson")
layered$objects$
district_level$objects
layered$objects$
district_level
layered$objects$
district_level
district_level <- fromJSON(file = "pakistan_districts.topojson")
pkg_chk_install <- function(package_name){
if (!(package_name %in% row.names(installed.packages()))){
install.packages(package_name)
}
}
pkg_chk_install("rjson")
library(rjson)
district_level <- fromJSON(file = "pakistan_districts.topojson")
prov_level <- fromJSON(file = "pakistan_prov.topojson")
layered <- district_level
layered[['objects']][['pakistan_prov']] <- prov_level$objects$pakistan_prov
layered$objects$
district_level
layered$objects$
district_level
layered$objects$
district_level
layered$objects$
prov_level
pkg_chk_install <- function(package_name){
if (!(package_name %in% row.names(installed.packages()))){
install.packages(package_name)
}
}
pkg_chk_install("rjson")
library(rjson)
district_level <- fromJSON(file = "pakistan_districts.topojson")
pkg_chk_install <- function(package_name){
if (!(package_name %in% row.names(installed.packages()))){
install.packages(package_name)
}
}
pkg_chk_install("rjson")
library(rjson)
district_level <- fromJSON(file = "pakistan_districts.topojson")
prov_level <- fromJSON(file = "pakistan_prov.topojson")
district_level
layered <- district_level
layered[['objects']][['pakistan_prov']] <- prov_level$objects$pakistan_prov
layered$objects$
prov_level
prov_level <- fromJSON(file = "pakistan_prov.topojson")
layered <- district_level
layered[['objects']][['pakistan_prov']] <- prov_level$objects$pakistan_prov
prov_level
prov_level
write(toJSON(layered), file = 'pak_dist_prov.topojson')
layered$arcs
dim(layered$arcs)
length(layered$arcs)
